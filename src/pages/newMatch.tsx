import { Form } from '../components/NewMatch.Form';

export function NewMatch() {
    const template = (
        <>
            <Form />
        </>
    );
    return template;
}

export default NewMatch;
